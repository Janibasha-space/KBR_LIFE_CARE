          {/* Doctors Carousel */}
          <View style={styles.doctorsSection}>
            <Text style={styles.sectionHeaderTitle}>Meet Our Expert Doctors</Text>
            <Text style={styles.sectionHeaderSubtitle}>Experienced specialists dedicated to your health</Text>
            
            <View style={styles.doctorCard}>
              <View style={styles.doctorImageContainer}>
                <Image
                  source={{ uri: doctors[currentDoctorIndex].image }}
                  style={styles.doctorImage}
                />
                <View style={styles.carouselControls}>
                  <TouchableOpacity
                    style={styles.carouselButton}
                    onPress={() => setCurrentDoctorIndex((prev) => (prev - 1 + doctors.length) % doctors.length)}
                  >
                    <Ionicons name="chevron-back" size={20} color="#4AA3DF" />
                  </TouchableOpacity>
                  
                  <TouchableOpacity
                    style={[styles.carouselButton, styles.playButton]}
                    onPress={() => setIsPlaying(!isPlaying)}
                  >
                    <Ionicons name={isPlaying ? "pause" : "play"} size={20} color="white" />
                  </TouchableOpacity>
                  
                  <TouchableOpacity
                    style={styles.carouselButton}
                    onPress={() => setCurrentDoctorIndex((prev) => (prev + 1) % doctors.length)}
                  >
                    <Ionicons name="chevron-forward" size={20} color="#4AA3DF" />
                  </TouchableOpacity>
                </View>
                
                <View style={styles.dotsContainer}>
                  {doctors.map((_, index) => (
                    <TouchableOpacity
                      key={index}
                      style={[
                        styles.dot,
                        index === currentDoctorIndex ? styles.activeDot : styles.inactiveDot
                      ]}
                      onPress={() => setCurrentDoctorIndex(index)}
                    />
                  ))}
                </View>
              </View>
              
              <View style={styles.doctorInfo}>
                <Text style={styles.doctorName}>{doctors[currentDoctorIndex].name}</Text>
                <Text style={styles.doctorCredentials}>{doctors[currentDoctorIndex].credentials}</Text>
                <Text style={styles.doctorRole}>{doctors[currentDoctorIndex].role}</Text>
                {doctors[currentDoctorIndex].fellowship && (
                  <Text style={styles.doctorFellowship}>{doctors[currentDoctorIndex].fellowship}</Text>
                )}
                
                <View style={styles.expertiseSection}>
                  <Text style={styles.expertiseTitle}>Areas of Expertise:</Text>
                  {doctors[currentDoctorIndex].expertise.slice(0, 3).map((item, index) => (
                    <View key={index} style={styles.expertiseItem}>
                      <Ionicons name="checkmark-circle" size={16} color="#4AA3DF" />
                      <Text style={styles.expertiseText}>{item}</Text>
                    </View>
                  ))}
                </View>
                
                <TouchableOpacity style={styles.appointmentBookButton}>
                  <Text style={styles.appointmentBookText}>Book Appointment</Text>
                </TouchableOpacity>
              </View>
            </View>
          </View>

          {/* Stats Section */}
          <LinearGradient
            colors={['#4AA3DF', '#2563EB']}
            style={styles.statsSection}
          >
            <View style={styles.statsGrid}>
              <View style={styles.statItem}>
                <Ionicons name="people" size={32} color="white" />
                <Text style={styles.statValue}>10,000+</Text>
                <Text style={styles.statLabel}>Patients Treated</Text>
              </View>
              <View style={styles.statItem}>
                <Ionicons name="trophy" size={32} color="white" />
                <Text style={styles.statValue}>15+</Text>
                <Text style={styles.statLabel}>Years Experience</Text>
              </View>
              <View style={styles.statItem}>
                <Ionicons name="medical" size={32} color="white" />
                <Text style={styles.statValue}>25+</Text>
                <Text style={styles.statLabel}>Expert Doctors</Text>
              </View>
              <View style={styles.statItem}>
                <Ionicons name="heart" size={32} color="white" />
                <Text style={styles.statValue}>98%</Text>
                <Text style={styles.statLabel}>Success Rate</Text>
              </View>
            </View>
          </LinearGradient>

          {/* Services Section */}
          <View style={styles.servicesSection}>
            <Text style={styles.sectionHeaderTitle}>Our Medical Services</Text>
            <Text style={styles.sectionHeaderSubtitle}>Comprehensive healthcare solutions for you and your family</Text>
            
            <View style={styles.servicesGrid}>
              {services.map((service) => (
                <TouchableOpacity key={service.id} style={styles.serviceCard}>
                  <LinearGradient
                    colors={service.colors}
                    style={styles.serviceIconContainer}
                  >
                    <Ionicons name={service.icon} size={28} color="#333333" />
                  </LinearGradient>
                  <Text style={styles.serviceTitle}>{service.title}</Text>
                  <Text style={styles.serviceDescription}>{service.description}</Text>
                </TouchableOpacity>
              ))}
            </View>
          </View>

          {/* Health Packages Section */}
          <View style={styles.healthPackagesSection}>
            <Text style={styles.sectionHeaderTitle}>Health Packages</Text>
            <Text style={styles.sectionHeaderSubtitle}>Comprehensive checkups tailored to your needs</Text>
            
            <ScrollView horizontal showsHorizontalScrollIndicator={false} style={styles.packagesScroll}>
              {healthPackages.map((pkg) => (
                <View key={pkg.id} style={[styles.packageCard, pkg.popular && styles.popularPackage]}>
                  {pkg.popular && (
                    <View style={styles.popularBadge}>
                      <Text style={styles.popularText}>Most Popular</Text>
                    </View>
                  )}
                  
                  <Text style={styles.packageName}>{pkg.name}</Text>
                  <Text style={styles.packagePrice}>{pkg.price}</Text>
                  
                  <View style={styles.featuresContainer}>
                    {pkg.features.map((feature, index) => (
                      <View key={index} style={styles.featureItem}>
                        <Ionicons name="checkmark-circle" size={16} color="#10B981" />
                        <Text style={styles.featureText}>{feature}</Text>
                      </View>
                    ))}
                  </View>
                  
                  <TouchableOpacity 
                    style={[styles.bookPackageButton, pkg.popular && styles.popularBookButton]}
                    onPress={() => setShowLoginModal(true)}
                  >
                    <Text style={[styles.bookPackageText, pkg.popular && styles.popularBookText]}>
                      Book Now
                    </Text>
                  </TouchableOpacity>
                </View>
              ))}
            </ScrollView>
          </View>

          {/* Popular Tests Section */}
          <View style={styles.popularTestsSection}>
            <Text style={styles.sectionHeaderTitle}>Popular Diagnostic Tests</Text>
            <Text style={styles.sectionHeaderSubtitle}>Quick and accurate test results</Text>
            
            <View style={styles.testsGrid}>
              {popularTests.map((test) => (
                <TouchableOpacity 
                  key={test.id} 
                  style={styles.testCard}
                  onPress={() => setShowLoginModal(true)}
                >
                  <Text style={styles.testName}>{test.name}</Text>
                  <Text style={styles.testPrice}>{test.price}</Text>
                  <View style={styles.bookTestButton}>
                    <Text style={styles.bookTestText}>Book</Text>
                  </View>
                </TouchableOpacity>
              ))}
            </View>
          </View>

          {/* Contact Section */}
          <View style={styles.contactSection}>
            <Text style={styles.sectionHeaderTitle}>Get In Touch</Text>
            <Text style={styles.sectionHeaderSubtitle}>We're here to help you 24/7</Text>
            
            <View style={styles.contactGrid}>
              <TouchableOpacity style={styles.contactCard} onPress={handleEmergencyCall}>
                <LinearGradient
                  colors={['#FEE2E2', '#FECACA']}
                  style={styles.contactIconContainer}
                >
                  <Ionicons name="call" size={24} color="#C62828" />
                </LinearGradient>
                <Text style={styles.contactTitle}>Phone</Text>
                <Text style={styles.contactText}>+91 8466 999 000</Text>
                <Text style={styles.contactSubtext}>24/7 Emergency</Text>
              </TouchableOpacity>

              <TouchableOpacity style={styles.contactCard} onPress={() => Linking.openURL('mailto:info@kbrhospitals.com')}>
                <LinearGradient
                  colors={['#E6F4FB', '#D7ECF9']}
                  style={styles.contactIconContainer}
                >
                  <Ionicons name="mail" size={24} color="#4AA3DF" />
                </LinearGradient>
                <Text style={styles.contactTitle}>Email</Text>
                <Text style={styles.contactText}>info@kbrhospitals.com</Text>
                <Text style={styles.contactSubtext}>support@kbrhospitals.com</Text>
              </TouchableOpacity>

              <View style={styles.contactCard}>
                <LinearGradient
                  colors={['#D1FAE5', '#A7F3D0']}
                  style={styles.contactIconContainer}
                >
                  <Ionicons name="location" size={24} color="#10B981" />
                </LinearGradient>
                <Text style={styles.contactTitle}>Location</Text>
                <Text style={styles.contactText}>Sangareddy</Text>
                <Text style={styles.contactSubtext}>Telangana, India</Text>
              </View>

              <View style={styles.contactCard}>
                <LinearGradient
                  colors={['#FEF3C7', '#FDE68A']}
                  style={styles.contactIconContainer}
                >
                  <Ionicons name="time" size={24} color="#F59E0B" />
                </LinearGradient>
                <Text style={styles.contactTitle}>Hours</Text>
                <Text style={styles.contactText}>Mon - Sun</Text>
                <Text style={styles.contactSubtext}>24/7 Open</Text>
              </View>
            </View>

            {/* Emergency Banner */}
            <LinearGradient
              colors={['#C62828', '#B71C1C']}
              style={styles.emergencyBanner}
            >
              <Text style={styles.emergencyTitle}>Emergency? Call Us Now!</Text>
              <Text style={styles.emergencySubtitle}>We're available 24/7 for medical emergencies</Text>
              <TouchableOpacity 
                style={styles.emergencyButton}
                onPress={handleEmergencyCall}
              >
                <Text style={styles.emergencyButtonText}>Call Emergency: +91 8466 999 000</Text>
              </TouchableOpacity>
            </LinearGradient>
          </View>